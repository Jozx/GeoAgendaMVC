@model IEnumerable<GeoAgenda.Models.Sucursal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Direccion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.contactoSucursal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.telefonoSucursal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.latitudSucursal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.longitudSucursal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.contactoSucursal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.telefonoSucursal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.latitudSucursal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.longitudSucursal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSucursal }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdSucursal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdSucursal })
        </td>
    </tr>
}

</table>
